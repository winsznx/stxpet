import { ImageResponse } from 'next/og';
export default function AppleIcon() { return new ImageResponse(<div style={{ width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#000', color: '#00ff94', fontSize: 100, fontWeight: 'bold' }}>SP</div>, { width: 180, height: 180 }); }